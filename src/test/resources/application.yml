spring:
  datasource:
    url: jdbc:h2:mem:hotel_db;TRACE_LEVEL_FILE=4;DB_CLOSE_DELAY=-1
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create